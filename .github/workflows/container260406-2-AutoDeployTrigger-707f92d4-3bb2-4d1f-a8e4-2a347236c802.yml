name: Trigger auto deployment for container260406-2

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/container260406-2-AutoDeployTrigger-707f92d4-3bb2-4d1f-a8e4-2a347236c802.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CONTAINER2604062_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINER2604062_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINER2604062_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINER2604062_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINER2604062_REGISTRY_PASSWORD }}
          containerAppName: container260406-2
          resourceGroup: Deleteme260406-2
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


